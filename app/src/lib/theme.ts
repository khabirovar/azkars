// Fixed indigo-night theme, independent of the user's Telegram light/dark
// setting — see index.css for the full palette. Must match --bg there so
// Telegram's native header/chrome doesn't seam against the page background.
export const THEME_BACKGROUND = "#12141c";
export const THEME_HEADER = "#12141c";
